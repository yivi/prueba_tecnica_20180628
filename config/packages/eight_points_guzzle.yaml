eight_points_guzzle:
    clients:
        xml_client:
            base_url: "http://feeds.spotahome.com/"

            options:
                timeout: 30

                # Configure headers.
                # More info: http://docs.guzzlephp.org/en/stable/request-options.html#headers
                headers:
                    User-Agent: "YiviSpotahomeCrawler/2.0"

            # Find plugins here:
            # https://github.com/8p/EightPointsGuzzleBundle#known-and-supported-plugins
            plugin: ~
